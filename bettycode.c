#include <stdio.h>
/**
 * add_numbers - used to add two numbers
 * @a: first number
 * @b: second number
 * Return: Always 0 (success)
 */
int add_numbers(int a, int b)
{
	int sum;

	sum = a + b;
	printf("Sum is %d", sum);
	return (0);
}
